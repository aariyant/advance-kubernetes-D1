apiVersion: batch/v1
kind: Job
metadata:
  name: sample-job
spec:
  template:
    spec:
      containers:
      - name: worker
        image: busybox
        command: ["sh", "-c", "echo 'Processing data...'; sleep 10; echo 'Done'"]
      restartPolicy: Never
  backoffLimit: 2
